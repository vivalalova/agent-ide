/**
 * Logger utility for E2E testing
 */

export class Logger {
  constructor(namespace) {
    this.namespace = namespace;
  }

  debug(...args) {
    // Silent in tests
  }

  info(...args) {
    // Silent in tests
  }

  warn(...args) {
    // Silent in tests
  }

  error(...args) {
    // Silent in tests
  }
}